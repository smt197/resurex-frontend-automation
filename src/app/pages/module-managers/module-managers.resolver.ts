import { createGenericResolver } from 'src/app/core/auto-generator/utils/resolver-generator';
import { MODULE-MANAGERS_CONFIG } from './module-managers.config';

export const module-managersResolver = createGenericResolver(MODULE-MANAGERS_CONFIG);
